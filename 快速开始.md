# Java 多版本配置快速开始

## ✅ 已完成的配置

1. ✅ 创建了 Java 版本管理脚本 (`~/.java_versions.sh`)
2. ✅ 配置了默认使用 Java 8
3. ✅ 更新了 spring-ai 启动脚本，自动切换到 Java 17

## 📥 下一步：安装 Java 17

### 安装步骤

1. **访问下载页面**：
   https://adoptium.net/temurin/releases/?version=17

2. **选择配置**：
   - Operating System: **macOS**
   - Architecture: **x64**  
   - Package Type: **JDK**
   - Version: **17 (LTS)**

3. **下载并安装**：
   - 下载 `.pkg` 文件
   - 双击安装包，按提示完成安装

4. **验证安装**：
   ```bash
   /usr/libexec/java_home -V
   ```
   应该能看到 Java 8 和 Java 17

## 🚀 使用方法

### 查看当前 Java 版本

```bash
show_java_version
```

### 切换到 Java 8（默认）

```bash
use_java8
```

### 切换到 Java 17

```bash
use_java17
```

### 运行 spring-ai 项目

```bash
cd /Users/daidasheng/opt_dev/workspace-service/cursor-AI/spring-ai
./start.sh
```

启动脚本会自动切换到 Java 17，无需手动切换。

## 📋 配置说明

- **默认版本**: Java 8（启动终端时自动设置）
- **管理脚本**: `~/.java_versions.sh`
- **配置文件**: `~/.zshrc`（已自动添加）

## ⚠️ 重要提示

1. 安装 Java 17 后，需要**重新打开终端**或运行 `source ~/.zshrc`
2. 默认使用 Java 8，不影响现有项目
3. spring-ai 项目启动时会自动切换到 Java 17

---

详细配置说明请查看: `多版本Java配置指南.md`
